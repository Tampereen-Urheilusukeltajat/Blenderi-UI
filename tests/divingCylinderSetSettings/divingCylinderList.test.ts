import { test } from '@playwright/test';
test.describe('Diving cylinder list', () => {
  // test.beforeEach(async ({ page }) => {
  //   await page.goto('/diving-cylinder-set');
  // });
  // test('Renders diving cylinder list', async ({ page }) => {
  //   await expect(page.locator('data-testid=common-table')).toBeVisible();
  // });
});
